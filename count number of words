import java.util.Scanner;
class Main
{
    public static void main(String [] args)
    {
        Scanner scanner=new Scanner (System.in);
        System.out.println(" Enter the words: ");
        String input = scanner.nextLine();
        if(input.trim().isEmpty()){
            System.out.println("The input string has no words");
        }else{
            String[] words=input.trim().split("\\s+");
            int wordCount=words.length;
            System.out.println("The number of words in a String is: "+ wordCount);
        }
        scanner.close();
        }
    }

output:  Enter the words: 
java is  platfrom independent programming language
The number of words in a String is: 6
